# Box64 DynaRec Debugging

## Isolating DynaRec bugs per library

When an application fails under Box64 with DynaRec enabled but works with `BOX64_DYNAREC=0`, the bug is in the code generated by DynaRec for some specific library. The goal is to find which library is the culprit without disabling DynaRec globally (which causes a large performance hit).

### Step 1 — confirm the bug is DynaRec-related

```bash
BOX64_DYNAREC=0 box64 <program>
```

If it works with `DYNAREC=0` and fails with `DYNAREC=1`, the problem is in DynaRec-generated code.

### Step 2 — get library load addresses

```bash
BOX64_LOG=2 box64 <program> 2>&1 | grep "Pre-allocated"
```

Typical output:
```
Pre-allocated 0x4e900 byte at 0x7fff0c000000 for /app/linux64/libsteam_api.so
Pre-allocated 0x3d6288 byte at 0x7fff0d000000 for /app/linux64/libRakNet64.so
Pre-allocated 0x57038 byte at 0x7fff0e000000 for /app/linux64/libZNetJNI64.so
Pre-allocated 0x461da0 byte at 0x7fff0f000000 for /app/linux64/libPZPopMan64.so
Pre-allocated 0x85090 byte at 0x7fff10000000 for /app/linux64/libPZPathFind64.so
Pre-allocated 0x2b9cc38 byte at 0x7fff11000000 for /app/linux64/steamclient.so
Pre-allocated 0x2b9cc38 byte at 0x7fff14000000 for /app/linux64/libsteam.so
```

Each library's range is: `base_address` to `base_address + size`.

To filter a specific group of libs:
```bash
BOX64_LOG=2 box64 <program> 2>&1 | grep -E "Pre-allocated.*/linux64"
```

### Step 3 — disable DynaRec for an address range

`BOX64_NODYNAREC` forbids DynaBlock creation within an address range (inclusive-exclusive):

```bash
# A single library:
BOX64_NODYNAREC=0x7fff11000000-0x7fff13b9cc38 box64 <program>

# A group of libraries (range covering all of them):
BOX64_NODYNAREC=0x7fff09000000-0x7fff17000000 box64 <program>

# Entire userspace address range (equivalent to DYNAREC=0, for testing):
BOX64_NODYNAREC=0x0000000000000000-0x00007fffffffffff box64 <program>
```

The range is calculated as:
- **start**: base address of the first lib to cover
- **end**: base address of the last lib + its size

### Step 4 — bisect to find the culprit library

Start with a range covering all suspected libraries. If it works, narrow it down:

```bash
# All suspected libs
BOX64_NODYNAREC=0x7fff0c000000-0x7fff17000000

# First half only
BOX64_NODYNAREC=0x7fff0c000000-0x7fff12000000

# Single lib only
BOX64_NODYNAREC=0x7fff0c000000-0x7fff0c060000
```

### Verifying that a per-library rcfile section was applied

The log confirms when an rcfile section was applied:
```
Applied [/steamclient.so] of range 0x7fff11000000:0x7fff13b9cc38
```

> **Limitation**: `BOX64_DYNAREC=0` cannot be scoped to a single library via rcfile — it is a process-global setting. Use `BOX64_NODYNAREC` with the address range for that purpose.

### Applying conservative DynaRec settings per library via rcfile

Even without being able to fully disable DynaRec per library, more conservative settings can be applied:

```ini
# ~/.box64rc or /etc/box64.box64rc
[/libsteam_api.so]
BOX64_DYNAREC_BIGBLOCK=0
BOX64_DYNAREC_CALLRET=0
BOX64_DYNAREC_STRONGMEM=3

[/steamclient.so]
BOX64_DYNAREC_BIGBLOCK=0
BOX64_DYNAREC_CALLRET=0
BOX64_DYNAREC_STRONGMEM=3
```

To point Box64 to a custom rcfile instead of the defaults, use `BOX64_RCFILE`:

```bash
BOX64_RCFILE=/path/to/custom.box64rc box64 <program>
```

### Notes on address stability

Load addresses are controlled by Box64 (`Pre-allocated ... at 0xXXXX`) and tend to be stable across runs within the same container, since Box64 manages library placement. Verify across two consecutive runs before hardcoding the address.

If the address varies (host ASLR), automate the capture:

```bash
ADDR=$(BOX64_LOG=2 box64 <program> 2>&1 | grep "Pre-allocated.*steamclient" | grep -oP '(?<=at )0x[0-9a-f]+')
SIZE=$(BOX64_LOG=2 box64 <program> 2>&1 | grep "Pre-allocated.*steamclient" | grep -oP '0x[0-9a-f]+(?= byte)')
# compute end and set BOX64_NODYNAREC=$ADDR-$(printf '0x%x' $((ADDR + SIZE)))
```

## Comparing DynaRec output against the interpreter instruction by instruction

To find exactly which instruction DynaRec is compiling incorrectly within a range:

```bash
BOX64_DYNAREC_TEST=0x7fff11000000-0x7fff13b9cc38 box64 <program>
```

Prints whenever DynaRec and interpreter diverge. Very slow — use with a small, targeted range.
